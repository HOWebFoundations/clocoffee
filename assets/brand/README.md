# Brand assets

**Empty on purpose — and that is a problem to fix.**

Five identity assets exist (cup mockup, two storefront renders, wordmark on espresso, isolated bloom)
but live only in a chat transcript. Drop the originals here at full resolution:

```
assets/brand/
  wordmark-espresso.png     # cream lettering on dark brown field
  bloom.png                 # isolated flower — cut to transparency, not the cream circle
  cup-mockup.png            # plain tumbler with wordmark
  storefront-a.png          # concept render — presentation only, do not ship
  storefront-b.png          # concept render — presentation only, do not ship
```

Once they are here, re-sample the palette and correct `docs/brand.md` §2. Every hex in that document
is currently eyeballed from a screenshot.
